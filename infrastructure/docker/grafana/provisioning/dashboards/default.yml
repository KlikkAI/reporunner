# ============================================
# Grafana Dashboard Provisioning
# Auto-loads dashboards from directory
# ============================================

apiVersion: 1

providers:
  - name: 'KlikkFlow Dashboards'
    orgId: 1
    folder: 'KlikkFlow'
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    allowUiUpdates: true
    options:
      path: /etc/grafana/dashboards
      foldersFromFilesStructure: false
