# Dependencies (these will be installed in Docker)
node_modules
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Build outputs (will be built in Docker)
dist
build
.next
.turbo
.cache

# Development files
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage
test-results
playwright-report
.nyc_output

# IDE
.vscode
.idea
*.swp
*.swo
*~

# OS
.DS_Store
Thumbs.db

# Logs
logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Documentation (keep README.md)
docs
*.md
!README.md
!CLAUDE.md

# Git
.git
.gitignore

# CI/CD
.github
.gitlab-ci.yml

# Development tools configuration (not needed in Docker)
.eslintrc*
.prettierrc*
biome.json
jest.config.*
vitest.config.*
playwright.config.*

# Reports
reports
bundle-analysis

# Temporary files
tmp
temp
.tmp

# IMPORTANT: Do NOT exclude these files - they are needed for Docker build
# - pnpm-lock.yaml (needed for --frozen-lockfile)
# - package.json files (explicitly copied in Dockerfile)
# - pnpm-workspace.yaml (needed for monorepo)
# - tsconfig.json files (needed for builds)
# - turbo.json (needed for Turborepo)

# Package manager lock files from other managers (we use pnpm)
yarn.lock
package-lock.json

# Development scripts (not needed in production image)
development
scripts
Makefile

# Docker files (no need to copy Docker files into Docker)
Dockerfile*
docker-compose*
.dockerignore

# Test data directories
test-performance-data-*
